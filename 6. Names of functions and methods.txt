1. hash_fun - getSlotWithHashFunc
// метод возвращает индекс слота в хэш-таблице

2. hash1, hash2 - getBitMaskWithHashFunc, getBitMaskWithExtraHashFunc
// метод возвращает битовую маску для индекса слота в фильтре Блюма

3. add - addString
// процедура добавляет строку в фильтр Блюма

4. isValue - containsString
// метод проверяет, содержится ли строка в фильтре Блюма

5. difference - getDifference
// метод возвращает разницу текущего множества и множества-параметра

6. union - getUnion
// метод возвращает объединение текущего множества и множества-параметра

7. intersection - getIntersection
// метод возвращает пересечение текущего множества и множества-параметра

8. size - getSetSize
// метод возвращает количество элементов в множестве

9. rotate - rotateWithStep
// функция вращает очередь по кругу на n элементов

10. sum - getSumsIfEqualLengths
// функция возвращает список, каждый элемент которого равен сумме соответствующих входных списков, если их длины равны

11. WeakVertices - getWeakVertices
// метод возвращает список узлов, не входящих ни в один треугольник графа

12. EvenTrees - getVerticesToDisconnect
// метод возвращает список вершин, между которыми нужно разорвать связь, чтобы в результате получился лес из четных деревьев


